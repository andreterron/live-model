// Thin local route adapter: the host owns its route tree and generated types,
// while the complete route implementation comes from the Explorer package.
export { ExplorerIndexRoute as default } from '@live-model/explorer';
